## 메소드 이름으로 쿼리 생성

<br/>

## 만약, 공통 기능이 아니라면?

예시를 보자면 이렇다.

```java
public interface MemberRepository extends JpaRepository<Member, Long> {

    List<Member> findByUsername(String username); // 이런 경우
}
```

위 코드를 보면, `Username` 이라는 것은 도메인에 특화된 경우이다.



### 결론은, 내가 따로 구현하지 않아도 `Spring-Data-Jpa`가 구현해준다.


<br/><br/>


## 메소드 이름으로 쿼리 생성

메소드 이름을 분석해서 JPQL 쿼리 실행한다


<br/>

### 이름과 나이를 기준으로 회원을 조회하려면?


`스프링 데이터 JPA` 사용하기 전 `순수 JPA`에서는 이렇게 작성하였다

```java
public List<Member> findByUsernameAndAgeGreaterThan(String username, int age) {
    return em.createQuery("select m from Member m where m.username = :username and m.age > :age")
            .setParameter("username", username)
            .setParameter("age", age)
            .getResultList();
}
```

<br/>

### `스프링 데이터 JPA`를 사용하면 이렇다

```java
public interface MemberRepository extends JpaRepository<Member, Long> {
    List<Member> findByUsernameAndAgeGreaterThan(String username, int age);
}
```

만약, `findByUsername2AndAgeGreaterThan` 라고 한다면 에러가 발생한다.

- 이유는, `username2` 라는 필드가 없기 때문이다

<br/><br/>

## 이렇게 동작이 가능한 이유는?

### 스프링 데이터 JPA가 제공하는 쿼리 메소드 기능



예:) `findHelloBy` 처럼 ...에 식별하기 위한 내용(설명)이 들어가도 된다.

- `전체조회` : `find…By`, `read…By`, `query…By`, `get…By`

    - `find…By` 에서 `By` 뒤에 이어, 작성하게 된다면 
    전체조회가 아니라 조건이 붙게 되는 것이다

- `COUNT` : `count…By` 반환타입 long

- `EXISTS` (있는가, 없는가) : `exists…By` 반환타입 boolean

- `삭제` : `delete…By`, `remove…By` 반환타입 long

- `DISTINCT` : `findDistinct`,  `findMemberDistinctBy`

- `LIMIT` : `findFirst3`, `findFirst`, `findTop`, `findTop3`

    - LIMIT에서의 숫자 3은 3개라는 뜻이다







<br/><br/>

>**Reference** <br/>[실전! 스프링 데이터 JPA](https://www.inflearn.com/course/%EC%8A%A4%ED%94%84%EB%A7%81-%EB%8D%B0%EC%9D%B4%ED%84%B0-JPA-%EC%8B%A4%EC%A0%84?_gl=1*1x5vsec*_ga*OTY2ODU2MjYxLjE2NzkwNjYzNDU.*_ga_85V6SRKGJV*MTY5MjMyMTczNi40MC4xLjE2OTIzNDAwNDguNTIuMC4w)


<br/>

## 궁금증!

```java
메서드 이름이 SQL 로 바뀌는 시점이 언제일까
```

앞의 스프링 데이터 JPA (사용 이유) 글에서 본 대로, 애플리케이션이 뜰 때 한 번만 한다.

```java
1. 프록시를 만들 때 인터페이스의 메서드를 전부 훑는다
2. 이름을 문법에 따라 쪼갠다
3. 엔티티의 필드와 맞는지 확인한다
4. JPQL 을 만들어서 보관해둔다
```

<br/>

그래서 오타가 있으면 애플리케이션이 아예 안 뜬다.

```java
Optional<Member> findByEmial(String email);
```

```java
PropertyReferenceException: No property 'emial' found for type 'Member'
```

<br/>

앞의 스프링 컨테이너 내부 동작 글에서 본 `문제가 있으면 뜨지도 않는다` 가 여기서도 이득이다.

앞의 MyBatis 적용 글에서 본 `Invalid bound statement` 와 같은 성격의 검사다.

<br/>

## 이름 규칙

```java
findBy      - 조회
countBy     - 개수
existsBy    - 있는지
deleteBy    - 삭제

And / Or                 findByNameAndAge
GreaterThan / LessThan   findByAgeGreaterThan
Between                  findByAgeBetween
Like / Containing        findByNameContaining
In                       findByIdIn
IsNull / IsNotNull       findByTeamIsNull
OrderBy                  findByNameOrderByAgeDesc
Top / First              findTop3ByOrderByAgeDesc
```

<br/>

`Containing` 이 편하다. `%` 를 자동으로 붙여준다.

```java
findByNameContaining("민")    ->    WHERE name LIKE '%민%'
```

<br/>

다만 앞의 DB 인덱스 글에서 본 대로 앞에 `%` 가 붙으면 인덱스를 못 쓴다.

```java
`--SCAN member
```

<br/>

앞이 고정이면 인덱스를 탄다.

```java
findByNameStartingWith("민")   ->   WHERE name LIKE '민%'
```

<br/>

## 이름이 길어지면 @Query 로 넘어간다

```java
List<Member> findByNameAndAgeGreaterThanAndCityAndDeletedFalseOrderByCreatedAtDesc(...);
```

<br/>

읽을 수가 없다. 조건이 셋을 넘으면 `@Query` 가 낫다.

```java
@Query("SELECT m FROM Member m WHERE m.name = :name AND m.age > :age AND m.deleted = false")
List<Member> search(@Param("name") String name, @Param("age") int age);
```

<br/>

`@Query` 도 시작할 때 문법 검사를 해준다. 오타가 있으면 안 뜬다.

<br/>

조건이 상황에 따라 붙었다 빠졌다 하면 이것도 한계다.

그때가 앞의 동적 쿼리 글에서 다루는 QueryDSL이 필요해지는 지점이다.

<br/>

## deleteBy 는 조심해야 한다

```java
void deleteByAge(int age);
```

<br/>

이건 `DELETE FROM member WHERE age = ?` 로 안 나간다.

```sql
SELECT * FROM member WHERE age = ?;      -- 먼저 조회하고
DELETE FROM member WHERE id = 1;         -- 하나씩 지운다
DELETE FROM member WHERE id = 2;
...
```

<br/>

앞의 영속성 전이 CASCADE 글에서 본 것과 같은 이유다.

영속성 컨텍스트에서 관리하려면 엔티티를 알아야 하기 때문이다.

<br/>

대량 삭제라면 `@Modifying` 을 쓴다.

```java
@Modifying(clearAutomatically = true)
@Query("DELETE FROM Member m WHERE m.age = :age")
int deleteAllByAge(@Param("age") int age);
```

<br/>

`clearAutomatically = true` 가 중요하다.

벌크 연산은 영속성 컨텍스트를 거치지 않고 DB로 바로 나가서,

1차 캐시에 옛 데이터가 그대로 남는다.

<br/>

앞의 영속성 컨텍스트 글에서 본 1차 캐시가 DB와 어긋나는 상황이다.

`clear` 로 비워줘야 다음 조회에서 새로 읽어온다.

<br/>

## 반환 타입에 따라 동작이 다르다

```java
Member findByEmail(String email);              // 없으면 null
Optional<Member> findByEmail(String email);    // 없으면 Optional.empty()
List<Member> findByAge(int age);               // 없으면 빈 리스트 (null 아님)
```

<br/>

`List` 는 절대 `null` 이 아니다. 그래서 `null` 검사를 안 해도 된다.

```java
List<Member> members = repository.findByAge(30);
if (members == null) { ... }        // 이 검사는 필요 없다
if (members.isEmpty()) { ... }      // 이걸 확인한다
```

<br/>

단건은 `Optional` 로 받는 편이 낫다.

앞의 래퍼 클래스 사용 이유는 글에서 본 대로, `null` 을 그냥 받으면 언젠가 `NullPointerException` 이 난다.

<br/>

그리고 결과가 둘 이상이면 `Optional` 이어도 예외가 난다.

```java
IncorrectResultSizeDataAccessException: query did not return a unique result: 2
```

<br/>

`email` 처럼 유일해야 하는 필드라면 앞의 relation database 글에서 본 대로

DB에 `UNIQUE` 제약을 걸어둬야 이 예외를 안 만난다.
