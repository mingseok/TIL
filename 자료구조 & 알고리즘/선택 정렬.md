## 선택 정렬

### ✔️[선택 정렬 구현 코드](https://github.com/mingseok/TIL/blob/main/code/selection.java)

<br/>

선택 정렬은 배열을 돌면서 가장 작은 값부터 하나씩 앞으로 차곡차곡 옮기는 것이다.

1. 배열의 각 셀을 왼쪽부터 오른쪽 방향으로 확인하면서 어떤 값이 최솟값인지 결정한다.
   
2. 한 셀씩 이동하면서 현재까지 가장 작은 값을 변수에 저장한다.
   
3. 변수에 들어 있는 값보다 작은 값이 들어 있는 셀을 만나면 변수가 새 인덱스를 가리키도록 값을 대체 한다.
   
4. 이제 최솟값이 어느 인덱스에 들어 있는지 알았으므로 그 인덱스의 값과 ‘패스스루’ 를 처음 시작했을 때의 값을 교환한다.

### 이렇게 반복하는 것이다.

<br/>

## 선택 정렬 실제로 해보기

예를 들어,

밑과 같은 배열이 있다고 생각해보자.

인덱스 0에 들어 있는 값을 확인하며 시작한다. 

```java
[ 4, 2, 7, 1, 3 ]
```

<br/>1단계. 현재 최솟값(즉, 4)과 2를 비교한다.



2는 4보다 작으므로 2가 현재까지의 최솟값이 된다.

<br/>

2단계. 현재 최솟값과 다음 값인 7을 비교한다. <br/> ------>7은 2보다 크므로 2가 여전히 최솟값이다.

<br/>

3단계. 현재 최솟값과 1을 비교한다. <br/> ------>1은 2보다 작으므로 1이 새로운 최솟값이 된다.

<br/>

4단계.  현재 최솟값인 1과 3을 비교한다. <br/>------>배열의 끝에 도달했으므로 전체 배열의 최솟값이 1로 결정됐다.

<br/>

5단계. 1이 최솟값이므로 1과 인덱스 0(패스스루를 시작했던 인덱스)의 값을 교환한다. <br/>------>이제 1은 배열 내에 올바른 위치에 있게 됐다. 

<br/>

<br/>두 번째 패스스루 부터는 첫 번째 셀, 즉 인덱스 0은 이미 정렬 됐으므로, 

두 번째 패스스루는 다음 셀인 인덱스 1부터 시작한다. 인덱스 1의 값은 숫자 2이며, <br/>이 값이 두 번째 패스스루의 현재 최솟값이다.

<br/>이렇게 두 번째 패스스루 → 세 번째 패스스루 → 등등 을 반복며 마지막 [ 1, 2, 3, 4, 7 ] 이 남는데

4가 여전히 이 패스스루의 최솟값이므로 어떤 교환도 필요 없으며, 4는 올바른 위치에 있게 됐다.

<br/>마지막 셀을 제외한 모든 셀이 올바르게 정렬 됐고, 

마지막 셀 역시 당연히 올바른 순서이므로 이제 전체 배열이 올바르게 정렬 됐다.

<br/>

<br/>

## 선택 정렬의 효율성

선택 정렬은 분명 버블 정렬보다 단계 수가 반 정도 적다.

즉, 선택 정렬이 두 배 더 빠르다.

| N개의 원소 | 버블 정렬에서 최대 단계 수 | 선택 정렬에서 최대 단계 수 |
| --- | --- | --- |
| 5 | 20 | 14(10번의 비교 + 4번의 교환) |
| 10 | 90 | 54(45번의 비교 + 9번의 교환) |
| 20 | 380 | 199(180번의 비교 + 19번의 교환) |
| 40 | 1560 | 819(780번의 비교 + 39번의 교환) |
| 80 | 6320 | 3239(3160번의 비교 + 79번의 교환) |


<br/><br/>

>**Reference**
<br/>제이 웬그로우 지음, 심지현 옮김 **『**누구나 자료구조와 알고리즘**』**, 길벗. <br/>
엔지니어대한민국 - https://www.youtube.com/watch?v=YbsQiiubO74

<br/>

## 궁금증!

```java
교환 횟수가 적은 게 언제 유리한가
```

옮기는 대상이 클 때다.

```java
int[] 정렬       - 교환이 싸다. 4바이트 세 번 대입
큰 객체 정렬     - 참조만 바꾸니 자바에서는 역시 싸다
C 의 구조체 배열 - 구조체 전체를 복사한다. 비싸다
```

<br/>

자바는 배열에 참조가 들어 있어서 교환 비용이 항상 같다.

그래서 이 장점이 잘 안 드러난다.

<br/>

C처럼 값을 직접 담는 언어에서는 차이가 크다.

<br/>

## 그래도 비교 횟수는 똑같다

앞의 버블 정렬 글에서 본 그 수치다.

```java
n=1000   버블=499500   선택=499500
```

<br/>

`n(n-1)/2` 로 같다.

<br/>

선택 정렬은 이미 정렬되어 있어도 이만큼 비교한다.

`최선도 O(n²)` 인 것이다.

<br/>

```java
버블 (개선판) 최선 O(n)
삽입          최선 O(n)
선택          최선 O(n²)
```

<br/>

선택 정렬만 최선이 없다.

최솟값을 찾으려면 끝까지 봐야 하기 때문이다.

<br/>

## 안정 정렬이 아니다

같은 값의 순서가 바뀔 수 있다.

```java
[3a, 3b, 1]

1 라운드: 최솟값 1 을 찾아서 0 번 자리와 교환
[1, 3b, 3a]      <- 3a 와 3b 의 순서가 뒤집혔다
```

<br/>

멀리 있는 것과 교환하니 중간에 있던 같은 값을 뛰어넘는 것이다.

<br/>

앞의 버블 정렬 글에서 본 안정성이 여기서 깨진다.

<br/>

연결 리스트로 구현하면 안정 정렬로 만들 수 있다.

교환 대신 노드를 떼어내서 앞에 붙이면 되기 때문이다.

<br/>

## 힙 정렬과 이어진다

선택 정렬의 문제는 `최솟값을 찾는 데 O(n)` 이 든다는 것이다.

<br/>

최솟값을 `O(log n)` 에 찾을 수 있으면 전체가 `O(n log n)` 이 된다.

<br/>

그게 힙이다.

```java
선택 정렬  - 매번 전체를 훑어서 최솟값을 찾는다.  O(n) × n = O(n²)
힙 정렬    - 힙에서 최솟값을 꺼낸다.             O(log n) × n = O(n log n)
```

<br/>

자바의 `PriorityQueue` 가 힙이다.

앞의 큐 글에서 다루는 그 자료구조다.

<br/>

`선택 정렬을 어떻게 빠르게 만들까` 를 고민한 결과가 힙 정렬인 셈이다.

<br/>

## 교환을 아예 안 하는 개선

```java
int minIdx = i;
for (int j = i + 1; j < n; j++) {
    if (a[j] < a[minIdx]) minIdx = j;
}
if (minIdx != i) swap(a, i, minIdx);      // 자기 자리면 교환 안 함
```

<br/>

이미 제자리에 있으면 교환을 건너뛴다.

<br/>

큰 차이는 아니지만, 정렬된 배열에서 교환이 0번이 된다.

<br/>

## 언제 쓰나

거의 안 쓴다.

<br/>

굳이 꼽자면 이런 조건일 때다.

```java
데이터가 아주 적다 (수십 개)
교환 비용이 비교 비용보다 훨씬 크다
추가 메모리를 못 쓴다
```

<br/>

세 번째는 삽입 정렬도 만족한다.

<br/>

앞의 Big_O 글에서 본 대로 `n` 이 작으면 뭘 써도 비슷하니,

읽기 쉬운 것을 쓰면 된다.

<br/>

실무에서는 `Arrays.sort()` 나 `list.sort()` 를 쓴다.

앞의 정렬(Sort) 글에서 본 `TimSort` 나 `DualPivotQuickSort` 가 돌아간다.

직접 구현할 이유가 거의 없는 것이다.
