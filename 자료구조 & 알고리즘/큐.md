## 큐의 개념 FIFO

먼저 들어간 친구가 먼저 나온다고 해서 First In First Out 이라고 한다. == **FIFO(피포)**

![이미지](/programming/img/qqq1.PNG)

<br/><br/>

## 큐 구현

![이미지](/programming/img/qqq2.PNG)

![이미지](/programming/img/qqq3.PNG)

<br/><br/>

## r-f 는 뭐야? 원소의 갯수이다.

```java
import java.util.Scanner;

class Queue {

  //        0 1 2 3 4 5 6 7
  // data   0 0 0 4 5 6
  //              f     r

  // f: 맨앞을 뜻함, r: 맨뒤를 뜻함 이렇게 지정해서 해준다.
  // 즉 앞에를 지웠다면, 뒤를 전부 한칸씩 땡기는게 아니라,
  // 그 지운 자리만 숫자 0으로 표시 해둔다.
  // 그리고 f를 증가 시켜가며 움직이는 것이다.

  int[] data = new int[100];
  int f = 0;
  int r = 0;
  int capacity = 0; // 최대 몇개의 원소를 넣을수 있느냐

  void create(int y) {
    capacity = y;
    f = 0;
    r = 0;

  }

  void push(int y) {
    //        0 1 2 3 4 5 6 7
    // data   1 2 0 0 0 0 0 0
    //        f
    //            r

    if (r - f >= capacity) { // 가득 찼다는 얘기이다. 
      System.out.println("Queue overflow!");

    } else {
      data[r++] = y;
    }

  }

  void pop() {
    if (r - f <= 0) { // 원소가 없다면
      System.out.println("Queue overflow!");

    } else {
      data[f] = 0;
      f++;
    }

  }

  int front() {
    // q의 가장 처음에 들어온 값을 반환하는 함수.
    // 단, 반환할 것이 없다면 -1을 반환

    if (r - f <= 0) {
      return -1;

    } else {
      return data[f];
    }

  }

  int size() {
    return r - f;

  }

}

public class aaa {

  public static void main(String[] args) {

    Queue q1 = new Queue();
    q1.create(3);

    q1.push(1);
    q1.push(2);
    q1.push(3);
    q1.push(4); // overflow
    q1.push(5); // overflow

    System.out.println(q1.front()); // 1

    q1.pop();
    q1.pop();

    System.out.println(q1.front()); // 3
    System.out.println(q1.size()); // 1

    q1.pop();
    q1.pop(); // underflow

    System.out.println(q1.size()); // 0

  }
}
```

<br/><br/>

## 원형 큐의 개념과 구현방법

## 원형 큐가 왜 필요한가??

이유는 선형 큐에 문제점이 있었으며, 이유는 공간 활용을 하지 못하는 것이었다.

즉, 앞에 공간이 충분히 비어 있는데도, f 와 r 이 계속해서 증가하기 때문에 앞에 공간이 

<br/>

있음에도 뒤 공간에 삽입해야 되는 것이었고 그렇게 계속 넘어가다 보면 

배열의 크기를 초과한다면 답이 없는 것이었다.

![이미지](/programming/img/qqq4.PNG)

<br/>

```java
import java.util.Scanner;

class Queue {

  //        0 1 2 3 4 5 6 7
  // data   0 0 0 4 5 6
  //              f     r

  // f: 맨앞을 뜻함, r: 맨뒤를 뜻함 이렇게 지정해서 해준다.
  // 즉 앞에를 지웠다면, 뒤를 전부 한칸씩 땡기는게 아니라,
  // 그 지운 자리만 숫자 0으로 표시 해둔다.
  // 그리고 f를 증가 시켜가며 움직이는 것이다.

  private final int MAX = 10; // 배열 크기의 최댓값 정할때는 이렇게 하기.

  int[] data = new int[MAX];
  int f = 0;
  int r = 0;
  int capacity = 0; // 최대 몇개의 원소를 넣을수 있느냐
  int numElement;

  void create(int y) {
    capacity = y;
    f = 0;
    r = 0;
    numElement = 0;
  }

  void push(int y) {
    //        0 1 2 3 4 5 6 7
    // data   1 2 0 0 0 0 0 0
    //        f
    //            r

    if (numElement >= capacity) { // 가득 찼다는 얘기이다.
      System.out.println("Queue overflow!");

    } else {
      data[r++] = y;

      if(r >= MAX) {
        r = 0;
      }

      numElement++;
    }
  }

  void pop() {
    if (numElement <= 0) { // 원소가 없다면
      System.out.println("Queue overflow!");

    } else {
      data[f] = 0;
      f++;

      if(f >= MAX) {
        f = 0;
      }

      numElement--;
    }
  }

  int front() {
    // q의 가장 처음에 들어온 값을 반환하는 함수.
    // 단, 반환할 것이 없다면 -1을 반환

    if (numElement <= 0) {
      return -1;

    } else {
      return data[f];
    }
  }

  int size() {
    return numElement;
  }
}

public class aaa {

  public static void main(String[] args) {

    Queue q1 = new Queue();
    q1.create(3);

    q1.push(1);
    q1.push(2);
    q1.push(3);
    q1.push(4); // overflow
    q1.push(5); // overflow

    System.out.println(q1.front()); // 1

    q1.pop();
    q1.pop();

    System.out.println(q1.front()); // 3
    System.out.println(q1.size()); // 1

    q1.pop();
    q1.pop(); // underflow

    System.out.println(q1.size()); // 0

  }
}
```

<br/><br/>

## 큐 클래스 불러와서 사용하기

## Queue 선언

```java
import java.util.LinkedList; //import
import java.util.Queue; //import
Queue<Integer> queue = new LinkedList<>(); //int형 queue 선언, linkedlist 이용
Queue<String> queue = new LinkedList<>(); //String형 queue 선언, linkedlist 이용
```

<br/>

## Queue 값 추가

```java
둘다 같은 것이다 암꺼나 사용해도 괜찮다.
queue.add(1);     // queue에 값 1 추가
queue.offer(3);   // queue에 값 3 추가
```

<br/>

## Queue 값 삭제 (=pop 하기)

```java
둘다 같은 것이다 암꺼나 사용해도 괜찮다. 그런데 poll() 을 많이 사용한다.
queue.remove();     // queue에 첫번째 값 제거 

queue.poll();       // queue에 첫번째 값을 반환한다. 
										// c언어에서 front()랑 같은 역할을 한다. 
										// 제거 비어있다면 null
```

<br/>

## Queue 에서 가장 먼저 들어간 값 출력

Queue에서 첫번째로 저장된 값을 참조하고 싶다면 peek()라는 메서드를 사용하면 됩니다.

```java
queue.peek();       // queue의 첫번째 값 참조
```

<br/>

## isEmpty() 메소드는 현재 큐가 비어있는지를 확인 할 때 쓰인다.

요소의 개수가 0개라면 비어있다는 뜻이므로, 비어있다면 true를, 비어있지 않다면 false를 반환한다.

```java
queue.isEmpty()
```
<br/>

## 궁금증!

```java
배열로 큐를 만들면 무엇이 문제인가
```

앞에서 빼면 나머지를 전부 당겨야 한다.

```java
[A, B, C, D]
A 를 뺀다 -> [B, C, D] 로 만들려면 세 칸을 옮겨야 한다
```

<br/>

앞의 노드 글에서 잰 그 비용이다.

```java
ArrayList 맨 앞 삽입 10만 번 = 451ms
```

<br/>

삭제도 같은 이유로 느리다.

<br/>

## 그래서 원형 큐를 쓴다

당기는 대신 시작 위치를 옮긴다.

```java
[_, B, C, D]
 ^front 가 1 로 이동
```

<br/>

배열 끝에 도달하면 처음으로 돌아간다.

```java
front = (front + 1) % capacity;
rear  = (rear + 1) % capacity;
```

<br/>

나머지 연산으로 한 바퀴 돌게 만드는 것이다.

<br/>

`ArrayDeque` 가 이 방식이다.

다만 `%` 대신 비트 연산을 쓴다.

```java
head = (head - 1) & (elements.length - 1);
```

<br/>

용량을 2의 거듭제곱으로 유지하면 `& (n-1)` 이 `% n` 과 같아진다.

앞의 비트 연산 글에서 본 그 성질이다.

나눗셈보다 비트 연산이 빨라서 이렇게 한다.

<br/>

## 가득 찬 것과 빈 것을 구분하는 문제

원형 큐에서 `front == rear` 이면 빈 건지 꽉 찬 건지 모른다.

<br/>

세 가지 해법이 있다.

```java
1. 한 칸을 비워둔다 -> rear + 1 == front 면 가득 참
2. 개수를 따로 센다
3. 가득 차면 배열을 늘린다
```

<br/>

`ArrayDeque` 는 3번이다. 꽉 차면 두 배로 늘린다.

<br/>

앞의 선형 리스트 글에서 본 `ArrayList` 의 1.5배와 다르다.

`ArrayDeque` 는 2의 거듭제곱을 유지해야 해서 두 배여야 하는 것이다.

<br/>

## Queue 인터페이스에 메서드가 두 벌이다

```java
add(e)     실패하면 예외        offer(e)   실패하면 false
remove()   비었으면 예외        poll()     비었으면 null
element()  비었으면 예외        peek()     비었으면 null
```

<br/>

왼쪽은 `Collection` 에서 물려받은 것이고, 오른쪽이 큐 전용이다.

<br/>

용량 제한이 있는 큐에서 `add` 는 예외를 던진다.

```java
IllegalStateException: Queue full
```

<br/>

앞의 체크 예외, 언체크(런타임) 예외 활용 글에서 본 기준으로 보면,

`큐가 가득 찬 것` 은 예상 가능한 상황이라 예외보다 반환값이 낫다.

<br/>

그래서 `offer` 를 쓰는 게 보통이다.

<br/>

## PriorityQueue 는 FIFO 가 아니다

```java
PriorityQueue<Integer> pq = new PriorityQueue<>();
pq.offer(3); pq.offer(1); pq.offer(2);
pq.poll();      // 1
```

<br/>

넣은 순서가 아니라 우선순위 순으로 나온다.

<br/>

이름에 `Queue` 가 붙어 있지만 큐라기보다 힙이다.

<br/>

앞의 선택 정렬 글에서 본 대로,

`매번 최솟값을 찾는` 일을 `O(log n)` 에 해준다.

<br/>

`toString()` 으로 찍으면 정렬된 순서가 아니다.

```java
System.out.println(pq);      // [1, 3, 2] 같은 게 나온다
```

<br/>

힙은 완전 이진 트리를 배열에 담은 것이라, 배열 순서가 정렬 순서가 아니기 때문이다.

<br/>

`꺼낼 때마다 최솟값` 만 보장하는 것이다.

<br/>

## LinkedList 도 Queue 를 구현한다

```java
Queue<Integer> q = new LinkedList<>();
```

<br/>

되긴 하는데 `ArrayDeque` 가 더 빠르다.

<br/>

앞의 자료구조란 글에서 본 이유다.

노드가 흩어져 있어서 CPU 캐시에 안 맞고, 노드 객체를 매번 만들어야 한다.

<br/>

`null` 을 넣을 수 있느냐가 다르다.

```java
LinkedList  - null 을 넣을 수 있다
ArrayDeque  - NullPointerException
```

<br/>

`ArrayDeque` 가 막은 이유가 있다.

`poll()` 이 비었을 때 `null` 을 돌려주는데,

`null` 을 넣을 수 있으면 `비었다` 와 `null 이 들어 있다` 가 구분이 안 된다.

<br/>

앞의 Map 정리 글에서 본 `ConcurrentHashMap` 이 `null` 을 금지한 것과 같은 이유다.

<br/>

## 큐가 쓰이는 곳

```java
BFS 탐색
작업 대기열
메시지 큐 (Kafka, RabbitMQ)
스레드 풀의 작업 큐
프린터 대기열
```

<br/>

앞의 멀티 쓰레드 글에서 본 스레드 풀도 안에 큐가 있다.

```java
new ThreadPoolExecutor(..., new LinkedBlockingQueue<>())
```

<br/>

스레드가 다 바쁘면 여기에 쌓인다.

<br/>

`BlockingQueue` 는 비었을 때 기다린다.

`poll()` 이 `null` 을 돌려주는 대신, 뭔가 들어올 때까지 스레드를 재운다.

<br/>

생산자와 소비자를 이어주는 표준적인 방법이다.
